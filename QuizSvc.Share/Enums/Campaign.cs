namespace QuizSvc.Share.Enums;

// trong .net enum thường có tiêu chuẩn là viết hoa chữ cái đầu (PascalCase), không uppercase toàn bộ 
public enum CampaignStatus
{
    Unknown = 0,
    Inactive = 1,
    Active = 2
}
